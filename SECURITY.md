# 安全政策

## 支持范围

安全修复优先面向默认分支上的最新 `0.1.x-alpha` 代码。Recalink 当前只按单用户、本机部署场景设计，不应直接暴露到公网。

## 报告安全问题

公开发布时，维护者必须先启用 GitHub Private vulnerability reporting。私密报告入口可用后，请通过仓库 Security 页面提交漏洞报告。

如果 Security 页面没有私密报告入口，请不要公开漏洞细节、令牌、真实书签、正文、数据库导出或其他敏感数据；只创建一条不含敏感内容的普通 Issue，请求维护者启用私密联系渠道。

报告应包含受影响版本、复现条件、实际影响和不含私人数据的最小复现。维护者确认问题前，请避免公开利用细节。

## 安全边界

- Docker Compose 默认只把 API 的宿主机端口映射到 `127.0.0.1`；应用进程本身监听容器内的 `0.0.0.0`，直接在宿主机运行或更改网络配置时，用户必须自行限制访问；
- PostgreSQL 和 Meilisearch 不映射到宿主机；
- 服务端抓取包含 DNS 与重定向阶段的 SSRF 检查；
- 扩展令牌不是多用户登录系统；
- 登录页面正文只在用户显式点击扩展收藏时读取；
- 用户配置的第三方 AI 服务不属于 Recalink 的信任边界。
